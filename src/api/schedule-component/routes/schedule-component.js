'use strict';

/**
 * schedule-component router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::schedule-component.schedule-component');
